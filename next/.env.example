# Database (Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/elite_realty"

# Supabase (client-safe; prefix with NEXT_PUBLIC_ for browser)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Gemini (server-only; do not prefix with NEXT_PUBLIC_)
GEMINI_API_KEY="your-gemini-api-key"
# Or use API_KEY if you prefer:
# API_KEY="your-gemini-api-key"
